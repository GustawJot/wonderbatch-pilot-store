# Secrets — the publishable token lives here. Never commit it.
.env
.env.*
!.env.example

node_modules/
*.log
.DS_Store

# The request-pacing tally (see src/pacing.ts) — per-machine runtime state.
.request-window.json
